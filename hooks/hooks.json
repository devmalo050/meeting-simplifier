{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "DATA_DIR=~/.claude/plugins/data/meeting-simplifier-meeting-simplifier; mkdir -p \"$DATA_DIR\"; if [ ! -f \"$DATA_DIR/.setup-complete\" ]; then bash ~/.claude/plugins/marketplaces/meeting-simplifier/scripts/setup.sh && touch \"$DATA_DIR/.setup-complete\"; fi"
          }
        ]
      }
    ]
  }
}
